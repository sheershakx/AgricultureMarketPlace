<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.agrimarket">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgriMarket"
        android:usesCleartextTraffic="true">
        <activity android:name=".activitypage.postDetail"></activity>
        <activity android:name=".activitypage.consumerDashboard" />
        <activity android:name=".activitypage.createProduct" />
        <activity android:name=".activitypage.createFarmer" />
        <activity android:name=".activitypage.farmerProfile" />
        <activity android:name=".activitypage.postProducts" />
        <activity android:name=".activitypage.farmerDashboard" />
        <activity android:name=".activitypage.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>